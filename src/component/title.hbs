<div>
    {{#if big}}
    <h2 class="mb-5 text-primary">{{data}}</h2>
    {{else}}
    <h3 class="mb-4 text-primary">{{data}}</h3>
    {{/if}}
</div>

{{!-- <style>
    h2 {
        color: green;
    }

    h3 {
        color: green;
    }
</style> --}}