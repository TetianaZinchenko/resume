{{!--
Це код container файлу для summary сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

{{!-- <div>
    {{#with main}}
    <main>
        <section class="mb-5">
            {{> title data=summary.title big=true}}

            <div>
                <p>
                    {{summary.text}}
                </p>
            </div>
        </section>

        {{#with experience}}
        <section>
            {{> title data=title big=false}}

            <div>
                <p>
                    {{text}}
                </p>
            </div>
        </section>
        {{/with}}
    </main>
    {{/with}}
</div> --}}

<div>
    {{#with main}}
    <main class="row h-100">

        <div class="col-6">

            <section class="card h-100 mb-5 p-4">

                {{#with summary}}

                {{> title data=title big=true}}

                <div>
                    <p>
                        {{text}}
                    </p>
                </div>

                {{/with}}

            </section>

        </div>

        <div class="col-6">

            <section class="card h-100 p-4">
                {{#with experience}}

                {{> title data=title big=false}}

                <div>
                    <p>
                        {{text}}
                    </p>
                </div>

                {{/with}}
            </section>

        </div>

    </main>
    {{/with}}
</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}